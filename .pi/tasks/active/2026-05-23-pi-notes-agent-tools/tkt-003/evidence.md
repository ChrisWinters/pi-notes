# tkt-003 Evidence

## Validation

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

Result: passed.

## Test coverage

- `tests/package-resources.test.ts` now checks that the bundled skill contains tool-first routing guidance, `notes_show` global-note example, destructive handoff example, and correct slash command wording.
