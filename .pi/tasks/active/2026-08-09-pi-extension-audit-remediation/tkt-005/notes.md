# tkt-005 notes — Align package, CI, and release contracts

## Objective

Implement `spec.md` section 9 and ST-005.

## Scope

- Align coding-agent/TypeBox peer and dev metadata; regenerate lockfile/tests.
- Inspect dry-run tarball resources and bundled dependencies.
- Use `npm ci` in CI.
- Use `release.published` and protected manual dispatch for publish.
- Add tag/version preflight while preserving provenance and least privilege.

## Boundaries

- Do not publish or push.
- Do not invent an external protected environment name without repository evidence; document human setup if needed.

## Execution notes

Pending execution.
