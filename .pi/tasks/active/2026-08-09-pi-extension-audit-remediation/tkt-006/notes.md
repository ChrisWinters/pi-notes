# tkt-006 notes — Reconcile docs and complete validation

## Documentation changes

- Updated README storage, tool overwrite, truncation, queue/cancellation, strict path safety, and mode behavior.
- Replaced the stale nonexistent plans index with links to durable public documentation.
- Rewrote architecture flow around typed outcomes, injected Pi/local mutation coordination, private output recovery, and mode adapters.
- Added a command surface/mode matrix, exact overwrite handoffs, tool limits/retention, and CLI exit guidance.
- Distinguished lexical validation, symlink/type rejection, canonical containment, residual same-user races, and full-permission extension trust in security docs.
- Documented host `CONFIG_DIR_NAME` roots, standalone `.pi` compatibility, strict path invariants, cancellation, and the cross-process CLI limitation in storage docs.
- Updated bundled skill root wording and added an Unreleased changelog section covering all remediation contracts.
- Refreshed scan-first `docs/agent-docs.yaml` with code-anchored storage, coordination, outcome, mode, package, and release context.
- Extended docs contract tests for mode, output limit, config directory, symlink/containment, cross-process, and stale-plan claims.

## Reconciliation result

All ST-001 through ST-006 acceptance criteria are represented by implementation, focused regression tests, and public documentation. No unresolved implementation gap or product question was found. The protected environment name remains intentionally external/undetermined and is accurately documented rather than invented.

## Boundaries

- `dist/` is generated by `npm run build` and is not tracked in this repository; the final build regenerated it locally for package/smoke validation.
- No publish, push, authentication, or external release configuration occurred.
