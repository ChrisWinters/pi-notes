---
title: todo
updated: 2026-05-03T01:27:59.882Z
---

## Todo

1. Adjust default note so first heading is ## h2 and add space before and after heading
2. Add shortcut command `/notes add <name> ...` do not show in help list or README.md. This is the same command as `/notes new <name> ...`
3. Add shortcut command `/notes list ...` do not show in help list or README.md. Thi sis the same command as `/notes ls ...`
4. Create tool functions that the agent use. Use `~/Projects/pi-extensions/pi-tasks/docs/agent-docs.yaml` to explore the pi-tasks project on how it creates tool functions. Explore Pi extension documentation before development. The tool functions allow the agent to execute pi-notes commands on user request. Update `skills/pi-notes/SKILL.md` so the agent knows when and how to use the skill and prompts the user may use to instruct the agent to use a particular tool.
5. Is this still relevant or solved by item #4? `.pi/tasks/project/pi-notes-cli-shim-observation.md`